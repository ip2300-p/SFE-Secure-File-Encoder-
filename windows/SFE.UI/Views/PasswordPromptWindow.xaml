<Window x:Class="SFE.UI.Views.PasswordPromptWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:SFE.UI"
        Title="{Binding [PasswordWindowTitle], Source={StaticResource Lang}}"
        Width="420"
        SizeToContent="Height"
        Background="{StaticResource SurfaceCrustBrush}"
        FlowDirection="{Binding [IsRTL], Source={StaticResource Lang}, Converter={StaticResource RtlFlow}}"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True">

    <Window.Resources>
        <ObjectDataProvider x:Key="Lang" ObjectType="{x:Type local:LangProvider}" MethodName="get_Instance"/>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- ═══ هدر با نام سناریو ═══ -->
        <Border Grid.Row="0" Background="{StaticResource SurfaceBaseBrush}"
                BorderBrush="{StaticResource SurfaceOverlayBrush}" BorderThickness="0,0,0,1"
                Padding="20,14">
            <TextBlock x:Name="ScenarioNameText"
                       Text="{Binding [ScenarioPrefix], Source={StaticResource Lang}}"
                       Style="{StaticResource SubtitleStyle}"/>
        </Border>

        <!-- ═══ فیلدهای رمز ═══ -->
        <StackPanel Grid.Row="1" Margin="24,20,24,20">

            <!-- رمز عبور اصلی -->
            <TextBlock Text="{Binding [Password], Source={StaticResource Lang}}"
                       Style="{StaticResource LabelStyle}" Margin="0,0,0,6"/>
            <Grid Margin="0,0,0,16">
                <PasswordBox x:Name="PwdBox"/>
                <TextBox x:Name="PwdBoxVisible" Visibility="Collapsed"/>
                <Button Click="TogglePwd_Click" HorizontalAlignment="Right"
                        Width="30" Height="30" Margin="0,0,3,0"
                        Background="Transparent" BorderThickness="0" Cursor="Hand">
                    <Path Data="M12,9A3,3 0 0,0 9,12A3,3 0 0,0 12,15A3,3 0 0,0 15,12A3,3 0 0,0 12,9M12,4.5C7,4.5 2.73,7.61 1,12C2.73,16.39 7,19.5 12,19.5C17,19.5 21.27,16.39 23,12C21.27,7.61 17,4.5 12,4.5Z"
                          Fill="{StaticResource TextSecondaryBrush}" Width="16" Height="16" Stretch="Uniform"/>
                </Button>
            </Grid>

            <!-- رمز دوم -->
            <StackPanel x:Name="SecondPwdPanel" Visibility="Collapsed">
                <TextBlock Text="{Binding [SecondPassword], Source={StaticResource Lang}}"
                           Style="{StaticResource LabelStyle}" Margin="0,0,0,6"/>
                <Grid Margin="0,0,0,8">
                    <PasswordBox x:Name="SecondPwdBox"/>
                    <TextBox x:Name="SecondPwdBoxVisible" Visibility="Collapsed"/>
                    <Button Click="ToggleSecondPwd_Click" HorizontalAlignment="Right"
                            Width="30" Height="30" Margin="0,0,3,0"
                            Background="Transparent" BorderThickness="0" Cursor="Hand">
                        <Path Data="M12,9A3,3 0 0,0 9,12A3,3 0 0,0 12,15A3,3 0 0,0 15,12A3,3 0 0,0 12,9M12,4.5C7,4.5 2.73,7.61 1,12C2.73,16.39 7,19.5 12,19.5C17,19.5 21.27,16.39 23,12C21.27,7.61 17,4.5 12,4.5Z"
                              Fill="{StaticResource TextSecondaryBrush}" Width="16" Height="16" Stretch="Uniform"/>
                    </Button>
                </Grid>
                <TextBlock Text="{Binding [SecondPasswordHint], Source={StaticResource Lang}}"
                           Style="{StaticResource MutedStyle}" TextWrapping="Wrap"/>
            </StackPanel>
        </StackPanel>

        <!-- ═══ دکمه‌ها ═══ -->
        <Border Grid.Row="2" Background="{StaticResource SurfaceBaseBrush}"
                BorderBrush="{StaticResource SurfaceOverlayBrush}" BorderThickness="0,1,0,0"
                Padding="20,14">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="12"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0" x:Name="CancelBtn"
                        Content="{Binding [Cancel2], Source={StaticResource Lang}}"
                        Style="{StaticResource SecondaryBtn}" Click="CancelBtn_Click"/>
                <Button Grid.Column="2" x:Name="RunBtn"
                        Content="{Binding [Continue], Source={StaticResource Lang}}"
                        Style="{StaticResource GreenBtn}" Click="RunBtn_Click"/>
            </Grid>
        </Border>
    </Grid>
</Window>